import { initInitiative } from "./combat-properties/init-initiative.js";

/**
 * Initialize all elements for the combat properties on the PC builder page.
 */
export const initCombatProperties = function() {
    initInitiative();
}